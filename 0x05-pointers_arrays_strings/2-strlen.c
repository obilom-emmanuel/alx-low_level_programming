#include "main.h"

/**
 * _strlen - A program that returns the length of a string
 * @s: string to be returned
 *
 * Return: strlen.
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; *s[c] != 0; c++)
	{
	}
	return (c);
}
