#include "main.h"

/**
 * puts_half - A function that prints half of a string
 * @str: half str to be printed
 *
 * Return: i
 */

void puts_half(char *str)
{
	int i = 0;

	while (*str != '\0')
	{
		i++;
		s++;
	}
	return (i);
}
